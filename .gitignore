# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Secrets / credentials
.env
credentials/
*.json.key
# GCP service account key (named with project ID)
portfolio-management-*.json

# PDF statements — downloaded transiently, never committed
statements/

# macOS
.DS_Store

# Claude Code local config
.claude/

# data/history.json and data/processed/ are intentionally committed
# They store parsed statement history needed by the CI pipeline